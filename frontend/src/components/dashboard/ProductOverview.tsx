import EmptyState from "../common/EmptyState";
export default function ProductOverview({ products = [] }) { return <div className="card h-100"><div className="card-header"><h2 className="card-title">Product Overview</h2></div>{products.length ? products.map(item => <div className="progress-container" key={item.id}><div className="progress-label-row"><span className="progress-label">{item.label}</span><span className="progress-value">{item.value}</span></div><div className="progress" role="progressbar"><div className="progress-bar bg-lime-accent" style={{width: `${item.progress || 0}%`}} /></div></div>) : <EmptyState message="No product data." />}</div>; }
