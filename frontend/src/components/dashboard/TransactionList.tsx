import EmptyState from "../common/EmptyState";
export default function TransactionList({ transactions = [] }) { return <div className="card h-100"><div className="card-header"><h2 className="card-title">Transaction</h2></div>{transactions.length ? <div className="transaction-list">{transactions.map(item => <div className="transaction-item" key={item.id}><div className="transaction-icon bg-forest-light text-lime"><i className={`bi ${item.icon || "bi-receipt"}`} /></div><div className="transaction-info"><div className="transaction-name">{item.name}</div><div className="transaction-date">{item.date}</div></div><div className={`transaction-amount ${item.amount >= 0 ? "text-success" : "text-main"}`}>{item.amount}</div></div>)}</div> : <EmptyState message="No transaction data." />}</div>; }
